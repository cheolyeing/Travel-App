import {Area, AreaContextType} from './type';

export const DEFAULT_AREA: Area = {
  lv1: {
    code: 1,
    name: '서울',
  },
};

export const DEFAULT_VALUE: AreaContextType = {
  area: DEFAULT_AREA,
};
